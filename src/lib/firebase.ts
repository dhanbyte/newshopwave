import type { Auth } from 'firebase/auth'
import type { Firestore } from 'firebase/firestore'

// Firebase disabled - using Clerk for authentication
export const auth: Auth | null = null
export const db: Firestore | null = null
export default null
